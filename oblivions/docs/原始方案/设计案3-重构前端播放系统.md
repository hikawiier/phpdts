# 设计案3：重构前端播放系统（v3）

> 本文档定义前端 `BattlePlayer` 的重构方案：从散落判断重构为按 `PlayScript` 分段执行的纯播放器，并清理旧播放逻辑死代码。
>
> **所属总纲**：[战斗演出三层架构设计案.md](战斗演出三层架构设计案.md)
> **依赖**：
> - [设计案1-重构后端battle_log.md](设计案1-重构后端battle_log.md)（已完成）
> - [设计案2-新建前端导演系统.md](设计案2-新建前端导演系统.md)（v3 已完成，导演模块已就位）
> - [设计案4-Turn-Round生命周期与存储设计.md](设计案4-Turn-Round生命周期与存储设计.md)（已完成）
> - 后端补 emit（本文档 §二，作为阻塞前置项）
>
> **更新记录**：
> - v3 基于 v2 + 评审反馈（P1-P10，除 P3）重写
> - 相对 v2 关键变化：后端补 `cleared_name`/`ambusher_name` emit；Round segment 可见性修复；`battleLogEntries` 类型改为 `DirectedEntry[]`；HP 条头部加 `v-if`；BattleModal `playBattleLog` 完整重构；`enemyName`/`enemyLocation` 更新点明确；`getSegmentDivider` 简化

---

## 一、设计目标

### 1.1 核心问题

当前 `battle.ts` 的播放链路是**旧格式 battlelog 的产物**，与设计案1/2 的新格式完全不兼容：

| 位置 | 旧逻辑 | 问题 |
|------|--------|------|
| `battle.ts:474` | `entries.filter(e => e.phase === 'excute')` | 新格式已无 `excute` phase → 过滤结果永远为空，**前端播不出任何战斗日志** |
| `battle.ts:478` | `buildPlayContext(npcPid)` → 查 API 取敌人名称/HP | 新格式 pre emit 已带 `target_name/target_hp`，不需要查 API |
| `battle.ts:483` | `rebuildInitialHpFromEntries` | 读 `entry.extra.actor_oldhp`（extra 已删除）→ 已改为 no-op |
| `battle.ts:491` | `action_id === 'unarmed_strike'` 碰撞动画判断 | 散落判断，应由导演 `decideAnimation` 集中处理 |
| `battle.ts:433` | `groupByEncounter(entries)` | 新格式由导演 `buildSegments` 按 Turn/Round 分段 |
| `BattleModal.vue:200` | `updateHpBars` 读 `entry.extra.target_newhp` | extra 已删除 → 已改为 no-op |
| `BattleModal.vue:250` | HP 条头部无条件显示 | 纯系统事件场景 HP 条残留显示 |
| `battle-templates.ts` | 按 `action_id` 索引模板，从 `ctx.npcName` 读名称 | 新格式按 `directedKind` 分发，名称从 entry 直接读 |

### 1.2 改动目标

1. **后端补 emit**：`combatant_cleared` 补 `cleared_name`，`ambush_battle_end` 补 `ambusher_name`
2. **播放器重写**：`fetchAndPlayBattleLog` 接入导演，`playBattleLogGroup` 替换为 `playScript`（按 `PlaySegment` 分段执行）
3. **BattleModal 改造**：`playBattleLog` 重构为分段感知，HP 条从 `hpSnapshot` 更新，HP 条头部加 `v-if="showHpBar"`，段分隔符插入
4. **battle-templates 改造**：按 `directedKind` 分发，从 entry 直接读 name/hp，删除 `BattlePlayContext` 类型
5. **类型更新**：`battleLogEntries` 改为 `DirectedEntry[]`，新增 `currentSegment` ref
6. **清理旧死代码**：删除 battle.ts 中旧播放逻辑

### 1.3 不在本次范围

- **状态机轮询**（`startNpcTurnRefresh`/`stopNpcTurnRefresh`）：有效代码，PROCESSING 状态轮询仍需要
- **command-queue.ts**：有效代码，PROCESSING 锁判断仍需要
- **player.ts 的 `isNpcActing`**：有效代码，是 PROCESSING 的派生
- **enemyLocation 获取**：保持现有方式（从 `enemies` API 获取），后端补 `target_pls` 字段留作未来优化
- **renderBattleEnd 区分玩家胜负**：统一显示"战斗结束"，不区分赢/输

---

## 二、后端前置改造（阻塞项）

### 2.1 combatant_cleared 补 cleared_name

**文件**：`oblivions/include/game/battle/battle.main.php:302-308`

`combatant_cleared` 的被清理者可能是 actor 也可能是 target，字段名用 `cleared_name`（不与 action entry 的 `target_name` 混淆）。`$target_data` 在 line 297 已 fetch，可直接取。

```php
// 改造前
$obl_battle_log->setPhase('combatant_cleared');
$obl_battle_log->emit([
    'cleared_pid' => (int)$pid,
    'reason'      => $reason,
], $reason === 'unknown');

// 改造后
$obl_battle_log->setPhase('combatant_cleared');
$obl_battle_log->emit([
    'cleared_pid'  => (int)$pid,
    'cleared_name' => $target_data['name'],
    'reason'       => $reason,
], $reason === 'unknown');
```

### 2.2 ambush_battle_end 补 ambusher_name

**文件**：`oblivions/include/game/battle/battle.entry.php:74-92`

两处 emit（step 5a/5b）都补 `ambusher_name`。`$actor` 就是突袭者，`$actor['name']` 可直接取。

```php
// step 5a
$obl_battle_log->setPhase('ambush_battle_end');
$obl_battle_log->emit([
    'ambusher_pid'  => (int)$actor['pid'],
    'ambusher_name' => $actor['name'],
    'reason'        => 'ambush_' . $ambusher_quit_flag,
]);

// step 5b
$obl_battle_log->setPhase('ambush_battle_end');
$obl_battle_log->emit([
    'ambusher_pid'  => (int)$actor['pid'],
    'ambusher_name' => $actor['name'],
    'reason'        => 'ambush_killed_all',
]);
```

### 2.3 前端类型同步

**文件**：`vex-vue/src/types/api.ts`

`BattleLogEntry` 新增两个字段：

```typescript
cleared_name: string | null;
ambusher_name: string | null;
```

### 2.4 后端改造验证清单

- [ ] `battle.main.php` combatant_cleared emit 补 `cleared_name`
- [ ] `battle.entry.php` step 5a ambush_battle_end emit 补 `ambusher_name`
- [ ] `battle.entry.php` step 5b ambush_battle_end emit 补 `ambusher_name`
- [ ] `vex-vue/src/types/api.ts` BattleLogEntry 新增 `cleared_name`/`ambusher_name`
- [ ] 触发战斗验证 JSON 含新字段

---

## 三、播放器重写

### 3.1 fetchAndPlayBattleLog 重写

```typescript
import { direct, extractNpcPid, collectAllLogIds, type PlayScript, type PlaySegment } from './battle-director';

async function fetchAndPlayBattleLog(): Promise<void> {
  if (isPlayingBattleLog.value) return;
  if (!currentGroomid.value || !currentPid.value) return;

  try {
    dataManager.invalidate('battle_log');
    const result = await dataManager.fetch('battle_log', true);
    if (result.status !== 'success' || !result.data) return;

    const entries = (result.data as { entries?: BattleLogEntry[] }).entries || [];
    if (entries.length === 0) return;

    isPlayingBattleLog.value = true;

    // 1. 导演编排（同步纯函数）
    const script = direct(entries);
    if (script.segments.length === 0) {
      await markBattleLogPlayed(currentGroomid.value, currentPid.value, collectAllLogIds(entries));
      return;
    }

    // 2. 提取 NPC PID（替代旧 groupByEncounter）
    const npcPid = extractNpcPid(script);

    // 3. 演员执行
    await playScript(script, npcPid);

    // 4. 标记已播放（基于原始 entries 的 log_id）
    const markResult = await markBattleLogPlayed(
      currentGroomid.value,
      currentPid.value,
      collectAllLogIds(entries),
    );
    if (!markResult.success) {
      console.warn('[Battle] markBattleLogPlayed returned failure:', markResult);
    }
  } catch (e) {
    console.error('[Battle] fetchAndPlayBattleLog error:', e);
    useToastStore().showToast('战斗数据异常，请刷新', 'error', 4000, false, 'battle-error');
  } finally {
    isPlayingBattleLog.value = false;
  }
}
```

### 3.2 playScript：按 PlaySegment 分段执行

```typescript
async function playScript(script: PlayScript, npcPid: number): Promise<void> {
  for (const segment of script.segments) {
    switch (segment.kind) {
      case 'phase0':
        await playPhase0Segment(segment, npcPid);
        break;
      case 'round':
        await playRoundSegment(segment, npcPid);
        break;
      case 'turn':
        await playTurnSegment(segment, npcPid);
        break;
      case 'battle_end':
        await playBattleEndSegment(segment, npcPid);
        break;
      case 'ambush_battle_end':
        await playAmbushBattleEndSegment(segment, npcPid);
        break;
    }
  }
}
```

### 3.3 各段播放函数

```typescript
/** Phase 0 段：突袭攻击 */
async function playPhase0Segment(segment: PlaySegment, npcPid: number): Promise<void> {
  // 从 entries 读取敌人名称
  updateEnemyNameFromSegment(segment);
  await refreshEnemyLocation(npcPid);
  await playSegmentInModal(segment, { npcPid, alwaysShowHeader: true });
}

/** Round 段：先攻掷骰 */
async function playRoundSegment(segment: PlaySegment, npcPid: number): Promise<void> {
  // round 段即使 entries 渲染为空也显示段分隔符
  await playSegmentInModal(segment, { npcPid, alwaysShowHeader: true });
}

/** Turn 段：单回合动作 */
async function playTurnSegment(segment: PlaySegment, npcPid: number): Promise<void> {
  // 从 entries 读取敌人名称
  updateEnemyNameFromSegment(segment);
  await refreshEnemyLocation(npcPid);

  // 碰撞动画（读 animation 字段，不判断 action_id）
  for (const e of segment.entries) {
    if (e.animation === 'collision') {
      dataManager.broadcast('battle:play-collision', { entry: e, npcPid });
      await sleep(COLLISION_ANIM_DURATION);
    }
  }

  // 模态框播放
  await playSegmentInModal(segment, { npcPid });

  // 伤害数字
  dataManager.broadcast('battle:play-damage-numbers', {
    entries: segment.entries,
    npcPid,
  });
}

/** Battle End 段 */
async function playBattleEndSegment(segment: PlaySegment, npcPid: number): Promise<void> {
  await playSegmentInModal(segment, { npcPid, isBattleEnd: true });
}

/** Ambush Battle End 段 */
async function playAmbushBattleEndSegment(segment: PlaySegment, npcPid: number): Promise<void> {
  await playSegmentInModal(segment, { npcPid, isBattleEnd: true });
}
```

### 3.4 playSegmentInModal

```typescript
interface SegmentPlayOptions {
  npcPid: number;
  /** 即使 entries 渲染为空也打开模态框（显示段分隔符） */
  alwaysShowHeader?: boolean;
  /** 战斗结束段（即使无渲染条目也打开） */
  isBattleEnd?: boolean;
}

async function playSegmentInModal(
  segment: PlaySegment,
  options: SegmentPlayOptions,
): Promise<void> {
  const rendered = segment.entries
    .map(e => ({ entry: e, html: renderDirectedEntryHtml(e, currentPid.value) }))
    .filter(r => r.html);

  // 无渲染条目时的处理：
  // - isBattleEnd：仍打开模态框（显示战斗结束文字）
  // - alwaysShowHeader：仍打开模态框（显示段分隔符）
  // - 其他：跳过
  if (rendered.length === 0 && !options.isBattleEnd && !options.alwaysShowHeader) return;

  currentSegment.value = segment;
  battleLogEntries.value = rendered.map(r => r.entry);
  battleModalOpen.value = true;

  await waitForModalClose();
}

async function waitForModalClose(): Promise<void> {
  await new Promise<void>((resolve) => {
    const timeout = setTimeout(() => {
      _modalResolve = null;
      resolve();
    }, MODAL_TIMEOUT);
    _modalResolve = () => {
      clearTimeout(timeout);
      resolve();
    };
  });
}
```

### 3.5 辅助函数：enemyName/enemyLocation 更新

```typescript
/** 从 segment entries 读取敌人名称（优先用 action entry 的 target_name） */
function updateEnemyNameFromSegment(segment: PlaySegment): void {
  const firstAction = segment.entries.find(e => e.directedKind === 'action');
  if (firstAction?.target_name) {
    enemyName.value = firstAction.target_name;
  } else if (firstAction?.actor_name && Number(firstAction.actor_type) > 0) {
    // NPC 是 actor 时，actor_name 就是敌人名称
    enemyName.value = firstAction.actor_name;
  }
}

/** 从 enemies API 获取敌人位置（替代旧 refreshContextFromApi 的位置部分） */
async function refreshEnemyLocation(npcPid: number): Promise<void> {
  if (npcPid <= 0) return;
  try {
    const enemiesResult = await dataManager.fetch('enemies', true);
    if (enemiesResult.status === 'success' && enemiesResult.data) {
      const enemies = (enemiesResult.data as { enemies?: Enemy[] }).enemies || [];
      for (const enemy of enemies) {
        if (parseInt(String(enemy.pid)) === npcPid) {
          enemyLocation.value = enemy.pls ?? null;
          return;
        }
      }
    }
  } catch (e) {
    console.error('[Battle] refreshEnemyLocation error:', e);
  }
}
```

### 3.6 新增状态

```typescript
/** 当前正在播放的段（供 BattleModal 读取 meta + entries） */
const currentSegment = ref<PlaySegment | null>(null);

/** battleLogEntries 类型改为 DirectedEntry[] */
import type { DirectedEntry } from './battle-director';
const battleLogEntries = ref<DirectedEntry[]>([]);
```

---

## 四、battle.ts 删除的旧逻辑

| 函数/状态 | 行号（参考） | 删除原因 |
|----------|------------|---------|
| `groupByEncounter` | 114-128 | 被导演 `extractNpcPid` + `buildSegments` 替代 |
| `playBattleLogGroup` | 469-517 | 被简化版 `playScript` 替代 |
| `buildPlayContext` | 577-594 | 后端 pre emit 已带 name/hp，不需要查 API |
| `refreshContextFromApi` | 539-570 | 同上（位置部分由 `refreshEnemyLocation` 替代） |
| `rebuildInitialHpFromEntries` | 596-614 | 已是 no-op，新方案由 `hpSnapshot` 替代 |
| `playContext` ref | 76 | 不再需要播放上下文 |
| `extractEnemyPid` | 96-104 | 被导演 `extractNpcPid` 替代 |

**保留的状态/函数**：
- `enemyName` / `enemyLocation`：仍需更新战斗 header，来源改为 `updateEnemyNameFromSegment` + `refreshEnemyLocation`
- `battleLogEntries` / `battleModalOpen`：仍驱动模态框（类型改为 `DirectedEntry[]`）
- `notifyModalClosed`：模态框关闭回调
- `sleep`：动画间隔
- 所有状态机轮询相关（`startNpcTurnRefresh`/`stopNpcTurnRefresh` 等）

---

## 五、BattleModal.vue 改造

### 5.1 playBattleLog 重构

当前 `playBattleLog`（line 102-192）从 `battleStore.battleLogEntries` + `battleStore.playContext` 读取数据。重构为从 `battleStore.currentSegment` 读取段信息，逐条播放，插入段分隔符。

```typescript
import type { DirectedEntry, PlaySegment } from '@/stores/battle-director';
import { renderDirectedEntryHtml } from '@/data/battle-templates';

async function playBattleLog(): Promise<void> {
  if (playing.value) {
    battleStore.notifyModalClosed();
    return;
  }
  playing.value = true;
  try {
    const segment = battleStore.currentSegment;
    const entries = battleStore.battleLogEntries as DirectedEntry[];
    if (!segment) {
      battleStore.notifyModalClosed();
      return;
    }
    // battle_end 段可能 entries 为空但仍需显示
    if (entries.length === 0 && segment.kind !== 'battle_end' && segment.kind !== 'ambush_battle_end') {
      battleStore.notifyModalClosed();
      return;
    }

    // 初始化 HP（从段首条 action entry 的 hpSnapshot 读 before 值）
    initHpFromSegment(segment, entries);

    // 清空正文
    displayedEntries.value = [];

    // 显示模态框
    overlayOpen.value = true;
    overlayClosing.value = false;
    await sleep(OVERLAY_FADE_IN);
    if (cancelRequested) return;

    // 段分隔符
    const divider = getSegmentDivider();
    if (divider) {
      displayedEntries.value.push({ html: divider.html, shown: false, isDivider: true });
      await nextTick();
      displayedEntries.value[displayedEntries.value.length - 1].shown = true;
      scrollToBottom();
      await sleep(ENTRY_INTERVAL / 2);
      if (cancelRequested) return;
    }

    // 逐条播放
    const sorted = [...entries].sort((a, b) => Number(a.log_id || 0) - Number(b.log_id || 0));
    for (const entry of sorted) {
      if (cancelRequested) return;
      const html = renderDirectedEntryHtml(entry, battleStore.currentPid);
      if (html) {
        displayedEntries.value.push({ html, shown: false, isDivider: false });
        await nextTick();
        await sleep(ENTRY_FADE_DELAY);
        displayedEntries.value[displayedEntries.value.length - 1].shown = true;
        scrollToBottom();
      }
      updateHpFromSnapshot(entry);
      await sleep(ENTRY_INTERVAL);
    }

    if (cancelRequested) return;
    await sleep(COMPLETE_HOLD);
    if (cancelRequested) return;

    overlayClosing.value = true;
    overlayOpen.value = false;
    await sleep(OVERLAY_FADE_OUT);
    overlayClosing.value = false;
    displayedEntries.value = [];

    battleStore.notifyModalClosed();
  } catch {
    battleStore.notifyModalClosed();
  } finally {
    playing.value = false;
  }
}

/** 从段首条 action entry 的 hpSnapshot 初始化 HP 条 */
function initHpFromSegment(segment: PlaySegment, entries: DirectedEntry[]): void {
  const firstAction = entries.find(e => e.directedKind === 'action' && e.hpSnapshot);
  if (firstAction?.hpSnapshot) {
    const snap = firstAction.hpSnapshot;
    if (Number(firstAction.actor_type) === 0) {
      // 玩家是 actor，target 是 NPC
      enemyName.value = firstAction.target_name ?? '敌人';
      enemyHp.value = snap.targetHpBefore;
      enemyMaxHp.value = snap.targetMaxHp;
      playerHp.value = snap.actorHpBefore;
      playerMaxHp.value = snap.actorMaxHp;
    } else if (Number(firstAction.target_type) === 0) {
      // NPC 是 actor，玩家是 target
      enemyName.value = firstAction.actor_name ?? '敌人';
      enemyHp.value = snap.actorHpBefore;
      enemyMaxHp.value = snap.actorMaxHp;
      playerHp.value = snap.targetHpBefore;
      playerMaxHp.value = snap.targetMaxHp;
    }
  }
}
```

### 5.2 HP 条更新

```typescript
/** 根据 DirectedEntry 的 hpSnapshot 更新 HP 条（替代旧 updateHpBars） */
function updateHpFromSnapshot(entry: DirectedEntry): void {
  if (!entry.hpSnapshot) return;
  const snap = entry.hpSnapshot;
  if (Number(entry.actor_type) === 0) {
    // 玩家是 actor，target 是 NPC
    enemyHp.value = snap.targetHpAfter;
    enemyMaxHp.value = snap.targetMaxHp;
    playerHp.value = snap.actorHpAfter;
    playerMaxHp.value = snap.actorMaxHp;
  } else if (Number(entry.target_type) === 0) {
    // NPC 是 actor，玩家是 target
    playerHp.value = snap.targetHpAfter;
    playerMaxHp.value = snap.targetMaxHp;
    enemyHp.value = snap.actorHpAfter;
    enemyMaxHp.value = snap.actorMaxHp;
  }
}
```

删除旧 `updateHpBars` 函数。

### 5.3 段分隔符

```typescript
/** 获取当前段的分隔符（无则返回 null） */
function getSegmentDivider(): { html: string } | null {
  const seg = battleStore.currentSegment;
  if (!seg) return null;
  switch (seg.kind) {
    case 'phase0':            return { html: '── 突袭 ──' };
    case 'round':             return { html: `── 第 ${seg.meta.roundNum ?? 0} 轮 ──` };
    case 'turn':              return null;  // turn 段不显示分隔符（由 round 段覆盖）
    case 'battle_end':        return { html: '── 战斗结束 ──' };
    case 'ambush_battle_end': return { html: '── 突袭结束 ──' };
    default:                  return null;
  }
}
```

分隔符作为 `DisplayedEntry`（`isDivider: true`）插入 `displayedEntries`，模板中已有的 `:class="{ 'turn-divider': entry.isDivider }"` 自动生效，无需额外模板改造。

### 5.4 HP 条显示控制

```typescript
/** HP 条是否显示（仅 turn/phase0 段显示） */
const showHpBar = computed(() => {
  const seg = battleStore.currentSegment;
  if (!seg) return false;
  return seg.kind === 'turn' || seg.kind === 'phase0';
});
```

### 5.5 模板改造

```vue
<!-- HP 条头部：加 v-if="showHpBar" -->
<div v-if="showHpBar" class="battle-modal-header">
  <!-- ... HP 条内容不变 ... -->
</div>

<!-- 正文：不变，displayedEntries 已包含分隔符条目 -->
<div ref="bodyRef" class="battle-modal-body">
  <div
    v-for="(entry, i) in displayedEntries"
    :key="i"
    class="battle-log-entry"
    :class="{
      shown: entry.shown,
      'turn-divider': entry.isDivider,
    }"
    v-html="entry.html"
  ></div>
</div>
```

---

## 六、battle-templates.ts 改造

### 6.1 按 directedKind 分发

```typescript
import type { DirectedEntry, DirectedKind } from '@/stores/battle-director';

const KIND_TEMPLATES: Record<DirectedKind, (e: DirectedEntry, playerPid: number) => string> = {
  action: renderAction,
  initiative: renderInitiative,
  flee: renderFlee,
  combatant_cleared: renderCombatantCleared,
  battle_end: renderBattleEnd,
  ambush_battle_end: renderAmbushBattleEnd,
  display: renderDisplay,
};

export function renderDirectedEntryHtml(entry: DirectedEntry, playerPid: number = 0): string {
  const template = KIND_TEMPLATES[entry.directedKind];
  return template ? template(entry, playerPid) : '';
}
```

### 6.2 各 kind 渲染函数

```typescript
function renderAction(e: DirectedEntry, _playerPid: number): string {
  const actor = e.actor_name || displayActorByType(e.actor_type);
  const target = e.target_name || displayTargetByType(e.target_type);
  const actorClass = Number(e.actor_type) === 0 ? 'yellow' : 'red';
  const actionName = e.action_id ?? '未知动作';

  if (actionName === 'unarmed_strike') {
    return (
      `<span class="${actorClass}">${escapeHtml(actor)}</span>` +
      `对<span class="red">${escapeHtml(target)}</span>` +
      `使用了空手攻击，造成 <span class="yellow">${e.effect_value}</span> 点伤害。`
    );
  }
  return `<span class="${actorClass}">${escapeHtml(actor)}</span>使用了${escapeHtml(actionName)}。`;
}

function renderInitiative(_e: DirectedEntry, _playerPid: number): string {
  // 先攻掷骰：不渲染为模态框条目（先攻顺序面板由 BattleModal 单独渲染）
  return '';
}

function renderFlee(e: DirectedEntry, _playerPid: number): string {
  const actor = e.actor_name || '未知';
  const actorClass = Number(e.actor_type) === 0 ? 'yellow' : 'red';
  return `<span class="${actorClass}">${escapeHtml(actor)}</span>成功逃离了战斗！`;
}

function renderCombatantCleared(e: DirectedEntry, _playerPid: number): string {
  const clearedName = e.cleared_name ?? '未知';
  const reason = e.reason ?? 'unknown';
  if (reason === 'death') {
    return `<span class="red">${escapeHtml(clearedName)}</span>被击倒。`;
  }
  if (reason === 'escaped') {
    return `<span class="yellow">${escapeHtml(clearedName)}</span>逃离了战场。`;
  }
  return '';
}

function renderBattleEnd(_e: DirectedEntry, _playerPid: number): string {
  // 统一显示"战斗结束"，不区分玩家胜负
  return `<span class="text-fg-dim">═══ 战斗结束 ═══</span>`;
}

function renderAmbushBattleEnd(e: DirectedEntry, _playerPid: number): string {
  const ambusherName = e.ambusher_name ?? '未知';
  const reason = e.reason ?? '';
  if (reason === 'ambush_killed_all') {
    return `<span class="yellow">═══ 突袭成功，${escapeHtml(ambusherName)} 杀光了所有敌人 ═══</span>`;
  }
  if (reason === 'ambush_dead') {
    return `<span class="red">═══ 突袭失败，${escapeHtml(ambusherName)} 被击倒 ═══</span>`;
  }
  if (reason === 'ambush_escaped') {
    return `<span class="yellow">═══ 突袭结束，${escapeHtml(ambusherName)} 已逃离 ═══</span>`;
  }
  return `<span class="text-fg-dim">═══ 突袭结束 ═══</span>`;
}

function renderDisplay(e: DirectedEntry, _playerPid: number): string {
  if (e.phase === 'ap_recover') return '';  // AP 恢复不渲染
  if (e.phase === 'execute_verify_failed') {
    const actor = e.actor_name || '未知';
    return `<span class="yellow">${escapeHtml(actor)}</span>的动作校验失败：${escapeHtml(e.reason ?? '')}`;
  }
  if (e.phase === 'middle_check_target_dead') return '';  // 由 combatant_cleared 承载
  return '';
}

function displayActorByType(actorType: number | null): string {
  if (Number(actorType) === 0) return '你';
  return '未知敌人';
}

function displayTargetByType(targetType: number | null): string {
  if (Number(targetType) === 0) return '你';
  if (Number(targetType) > 0) return '未知敌人';
  return '';
}
```

### 6.3 删除的类型/函数

- `BattlePlayContext` 接口
- `BattleLogTemplate` 接口
- `BATTLE_TEMPLATES` 映射
- `renderBattleLogEntryHtml` 函数
- `displayActor` / `displayTarget` 函数

---

## 七、实施步骤

### 阶段0：后端补 emit（阻塞项）

1. `battle.main.php` combatant_cleared emit 补 `cleared_name`
2. `battle.entry.php` step 5a/5b ambush_battle_end emit 补 `ambusher_name`
3. `vex-vue/src/types/api.ts` BattleLogEntry 新增 `cleared_name`/`ambusher_name`
4. 验证：type-check 通过

### 阶段1：battle-templates.ts 改造

1. 新增 `import type { DirectedEntry, DirectedKind } from '@/stores/battle-director'`
2. 定义 `KIND_TEMPLATES` 映射 + 各 kind 渲染函数
3. 新增 `renderDirectedEntryHtml` 函数
4. 删除 `BattlePlayContext` / `BattleLogTemplate` / `BATTLE_TEMPLATES` / `renderBattleLogEntryHtml` / `displayActor` / `displayTarget`
5. 验证：type-check 通过

### 阶段2：battle.ts 播放器重写

1. 新增 `import { direct, extractNpcPid, collectAllLogIds, type PlayScript, type PlaySegment, type DirectedEntry } from './battle-director'`
2. `battleLogEntries` 类型改为 `ref<DirectedEntry[]>`
3. 新增 `currentSegment` ref
4. 新增 `playScript` + 各段播放函数 + `playSegmentInModal` + `waitForModalClose`
5. 新增 `updateEnemyNameFromSegment` / `refreshEnemyLocation`
6. 重写 `fetchAndPlayBattleLog`
7. 删除 `groupByEncounter` / `playBattleLogGroup` / `buildPlayContext` / `refreshContextFromApi` / `rebuildInitialHpFromEntries` / `playContext` / `extractEnemyPid`
8. 清理 import + return 对象
9. 验证：type-check 通过

### 阶段3：BattleModal.vue 改造

1. 新增 `import type { DirectedEntry, PlaySegment } from '@/stores/battle-director'`
2. import 从 `renderBattleLogEntryHtml` 改为 `renderDirectedEntryHtml`
3. 重构 `playBattleLog`（分段感知 + 段分隔符 + `initHpFromSegment`）
4. 新增 `updateHpFromSnapshot`（替代 `updateHpBars`）
5. 新增 `getSegmentDivider`
6. 新增 `showHpBar` computed
7. 模板 HP 条头部加 `v-if="showHpBar"`
8. 删除 `updateHpBars` + 旧 `playContext` 引用
9. 验证：type-check 通过

### 阶段4：MCP 浏览器测试

1. 启动 dev server
2. 触发战斗（ambush 突袭 → 标准战斗 → 战斗终结）
3. 验证：
   - Phase 0 突袭段：分隔符 + 动作条目 + HP 条
   - Round 段：分隔符显示（即使 initiative_roll 渲染为空）
   - Turn 段：HP 条 + 碰撞动画 + 动作条目 + 伤害数字
   - combatant_cleared：显示 cleared_name（非"未知"）
   - battle_end：显示战斗结束 + 退出战斗模式
   - ambush_battle_end：显示 ambusher_name + 退出战斗模式
   - HP 条时序正确（before → after）
   - 纯系统事件段 HP 条不显示
4. 回归测试：探索模式 / 命令队列

---

## 八、边界情况

### 8.1 enemyLocation 获取

保持从 `enemies` API 获取（`refreshEnemyLocation`），后端补 `target_pls` 留作未来优化。

### 8.2 空 segment

- round 段只有 initiative_roll（渲染为空）：`alwaysShowHeader: true` 确保分隔符显示
- battle_end 段无渲染条目：`isBattleEnd: true` 确保模态框打开
- 其他段无渲染条目：跳过

### 8.3 多回合合并播放

`direct(entries)` 一次性编排，`playScript` 逐段播放，多个 round/turn segment 自然形成多个模态框。

### 8.4 HP 条初始化

从 turn/phase0 段首条 action entry 的 `hpSnapshot.actorHpBefore`/`targetHpBefore` 读取（`initHpFromSegment`）。

### 8.5 模态框超时兜底

保留 `MODAL_TIMEOUT`（30s）超时兜底。

### 8.6 旧函数引用检查

`renderBattleLogEntryHtml` 改为 `renderDirectedEntryHtml` 后，检查是否有其他文件引用旧函数名。如有，更新引用。

---

## 九、验证检查清单

### 后端补 emit
- [ ] `combatant_cleared` emit 补 `cleared_name`
- [ ] `ambush_battle_end` emit（5a/5b）补 `ambusher_name`
- [ ] `BattleLogEntry` 类型新增 `cleared_name`/`ambusher_name`

### battle-templates.ts
- [ ] `KIND_TEMPLATES` 映射新增（按 directedKind 分发）
- [ ] 各 kind 渲染函数实现
- [ ] `renderDirectedEntryHtml` 新增
- [ ] 旧类型/函数删除
- [ ] `renderCombatantCleared` 读 `e.cleared_name`
- [ ] `renderAmbushBattleEnd` 读 `e.ambusher_name`

### battle.ts
- [ ] `battleLogEntries` 类型改为 `DirectedEntry[]`
- [ ] `currentSegment` ref 新增 + 导出
- [ ] `playScript` + 各段播放函数新增
- [ ] `playSegmentInModal` + `waitForModalClose` 新增
- [ ] `updateEnemyNameFromSegment` / `refreshEnemyLocation` 新增
- [ ] `fetchAndPlayBattleLog` 重写
- [ ] 旧函数/状态删除
- [ ] return 对象清理

### BattleModal.vue
- [ ] `playBattleLog` 重构（分段感知）
- [ ] `initHpFromSegment` 新增
- [ ] `updateHpFromSnapshot` 新增（替代 `updateHpBars`）
- [ ] `getSegmentDivider` 新增
- [ ] `showHpBar` computed 新增
- [ ] 模板 HP 条头部加 `v-if="showHpBar"`
- [ ] `updateHpBars` 删除

### 验证
- [ ] TypeScript 类型检查通过
- [ ] MCP 浏览器测试：Phase 0 突袭段
- [ ] MCP 浏览器测试：Round 段分隔符可见
- [ ] MCP 浏览器测试：Turn 段 HP 条 + 碰撞动画 + 伤害数字
- [ ] MCP 浏览器测试：combatant_cleared 显示 cleared_name
- [ ] MCP 浏览器测试：battle_end 退出战斗模式
- [ ] MCP 浏览器测试：ambush_battle_end 显示 ambusher_name + 退出战斗模式
- [ ] MCP 浏览器测试：HP 条时序（before → after）
- [ ] MCP 浏览器测试：纯系统事件段 HP 条不显示
- [ ] 回归测试：探索模式 / 命令队列

---

## 附录：v2 → v3 变更说明

| 变更项 | v2 | v3 |
|--------|----|----|
| combatant_cleared emit | 无 cleared_name | 后端补 `cleared_name`（P1） |
| ambush_battle_end emit | 无 ambusher_name | 后端补 `ambusher_name`（P10） |
| Round segment 可见性 | 可能完全不可见 | `alwaysShowHeader` 确保分隔符显示（P2） |
| enemyName 更新位置 | 缺失 | `updateEnemyNameFromSegment` 明确（P4） |
| enemyLocation | 缺失 | `refreshEnemyLocation` 从 enemies API 获取（P4） |
| battleLogEntries 类型 | `BattleLogEntry[]` | `DirectedEntry[]`（P5） |
| getSegmentDivider | 返回 `{ html, shown }` | 简化为 `{ html } \| null`，shown 沿用 DisplayedEntry 淡入机制（P6） |
| turn-divider class | 集成未说明 | 分隔符作为 DisplayedEntry(isDivider:true) 插入，模板现有 class 自动生效（P7） |
| HP 条头部 | 无条件显示 | 加 `v-if="showHpBar"`（P8） |
| BattleModal playBattleLog | 未重构 | 完整重构为分段感知（P9） |
| renderBattleEnd | — | 不区分玩家胜负，统一显示"战斗结束"（P3 不处理） |
