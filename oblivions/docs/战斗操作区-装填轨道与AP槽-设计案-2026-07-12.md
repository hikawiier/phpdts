# 战斗操作区 — 装填轨道与 AP 槽设计案

> 日期：2026-07-12（v2 修订：2026-07-12，修正与现有协议冲突、删除键盘快捷键、补充升格分级；v3 修订：2026-07-12，布局调换——装填轨道在上、技能库在下；技能库改为标签页筛选）
> 范围：`vex-vue/src/components/battle/PreloadArea.vue` 及其样式、`QueueItem` 数据结构、与 `AimMode` 的协议扩展。
> 上游约束：`oblivions/docs/前端美学风格-设计案.md`、`oblivions/docs/demos/ui-style-blended.html`。
> 性质：完整重构设计，不是补丁。
>
> **键盘快捷键策略声明**：本设计案**不引入任何键盘快捷键**。当前仓库已存在 3 处独立的 `keydown` 监听（`App.vue` 全局、`useMapInteraction`、`AimMode`），且各模块对 `ESC` / `Enter` / 方向键存在语义重叠。若在 PreloadArea 内单独引入快捷键，需为每个新模块人工设置屏障，违反"框架先行"原则。
>
> 键盘快捷键应作为**前端基础框架的一部分**统一引入：在所有前端功能落地后，建立 `useBattleKeyboard` composable 或统一的 `KeyboardManager`，明确分层优先级（BattleModal > AimMode > PreloadArea > Map），由该层统一下派事件。在此之前，本设计案的所有交互均通过鼠标/触摸完成。

---

## 〇、修订摘要（v2 + v3）

本设计案 v1 经与实际代码核对后发现 5 处关键错误和 4 处遗漏，已在 v2 中修正；v3 追加 2 处改动（布局调换、技能库标签页筛选）：

1. **§2.2 半成品动作与现有协议冲突**：v1 假设"半成品项已入队"与现有 `enterAimMode` 协议不兼容（`prefixActions` 会错误包含 pending 项）。v2 改为"虚拟 pending 项不入队"方案。
2. **§5.1 键盘控制整节删除**：见上方声明。
3. **§7.1.4 超载 AP 禁用机制修正**：v1 声称通过 `executeBlock` 实现，但实际 `command-queue.ts` 的 5 层锁不含 AP 检查。v2 改为 PreloadArea 内部 `isOverload` 派生状态。
4. **§6.1 派生字段改为 computed**：v1 假设的缓存字段会破坏 Vue 响应式。v2 改为 `queueWithCost` computed 实时计算，不污染 `QueueItem` 数据结构。
5. **§6.1 submitted 状态钩子补充**：v1 未定义进入/退出时机。v2 明确通过 `isPlayerTurn` watch 实现。
6. **§2.2 重新瞄准流程的 `aimTargetingStore.enter` 协议修正**：v1 声称需新增 `excludeItemId` 参数，实际无需——PreloadArea 先把该项从 `queue` 中移除，再传**移除后的** `queue.value` 即可，无需修改 store。
7. **§3.4 Breadcrumb sticky 容器约束补充**：v1 未考虑父容器 `overflow-y-auto` 的影响。
8. **§7.1.1 move apCost 实际语义补充**：`Math.max(baseCost, ...)` 下限保护、`movePower` 字段复用语义。
9. **§10 升格与边界分级矩阵补充**：将 4 类内容升格为可复用基准框架，6 类内容明确为边界案例。
10. **§3.1 布局调换（v3）**：v2 布局"技能库在上、装填轨道在下"调换为"装填轨道（信息区）在上、技能库（操作区）在下"，与探索页面"信息在上、操作在下"的操作习惯同步。涉及 §3.1 ASCII 图、§3.4 sticky 语义、§8 阶段 2 实现顺序的同步更新。
11. **§3.2 技能库改为标签页筛选（v3）**：v2 的"分组渲染"（按分类切片成多块）改为"标签页筛选"（顶部 5 标签 + 扁平列表 + 项尾小标签）。理由：分组渲染无筛选能力，标签页提供主动筛选且为未来技能数扩展预留交互骨架。默认选中"全部"保持全局视野。激活态样式先用简单下划线，后续调整为方案 C 硬投影。涉及 §3.1 ASCII 图、§4.2 样式、§8 阶段 2 实现的同步更新。

---

## 一、设计目标

围绕 8 个核心问题做整体重构，而非逐项打补丁：

| 问题编号 | 现状 | 本设计案归因 |
|---------|------|------------|
| B1 | 技能列表无分类平铺 | 信息架构缺层级，技能库缺分类基准 |
| B3 | 进入瞄准后无 breadcrumb | 瞄准是"半成品动作"的中间态，未被建模为队列项状态 |
| B4 | 队列项无法重新瞄准，只能删除重加 | 装填轨道的项缺"重新进入半成品态"的能力 |
| B5+ | 只有"清空全部"和"删除某项" | 队列缺原子操作层级（撤销末项、删除指定、修改目标、调整顺序） |
| C2 | AP 进度条无分段刻度 | "进度条"语言本身错误，AP 是离散预算，不是连续血量 |
| D2 | 队列项无序号 | 装填是时序计划，但视觉上时序不可见 |
| D3 | 队列项无 finisher 标记 | 时序约束（终结技必须在末尾）不可见 |
| D4 | 队列项无拖拽重排 | 时序计划应可调整，但调整能力完全缺失 |
| D5 | 队列头无总 AP 徽标 | 总成本需心算，缺聚合信息 |

**重构基线**：把"装填队列"从"已选技能列表"重新定义为"时序动作轨道"。这一概念转换后，B1/B3/B4/D2/D3/D4/D5 都是这一概念的必然视觉/交互要求，B5+ 是轨道操作的原子能力，C2 是配套预算显示的同步重设计。

---

## 二、概念提炼

### 2.1 时序轨道（替代"已选列表"）

**问题本质**：当前 `queue: QueueItem[]` 数据上是有序数组，但 UI 只把它渲染成"已选技能的纵向列表"，时序信息被抹平。

**新基准**：装填队列是玩家规划的**本回合动作时序**，按顺序入队 → 按顺序执行。所有视觉/交互都围绕"时序可见、可调"展开：

- 序号必须显式可见（D2）
- 时序约束（finisher 必须末尾、CD 技能只能出现一次）必须可见（D3）
- 时序必须可调整（D4 拖拽）
- 总成本必须聚合显示（D5）

**概念分级**：模块 → 基准框架（装填轨道的时序语义）

### 2.2 半成品动作（替代"独立瞄准模式"）

**问题本质**：当前进入瞄准模式后，PreloadArea 没有任何视觉反馈表明"正在为某技能选目标"。瞄准似乎是一个独立"模式"，而非队列项的中间态。

**现有协议约束**（v1 未察觉，v2 修正）：实际代码 [PreloadArea.vue:285-293](file:///d:/wamp64/www/phpdts/vex-vue/src/components/battle/PreloadArea.vue#L285-L293) `onSkillClick` 中，`aimType=tile/enemy` 时**不**立即入队，而是直接 `enterAimMode`。`enterAimMode` [PreloadArea.vue:319-334](file:///d:/wamp64/www/phpdts/vex-vue/src/components/battle/PreloadArea.vue#L319-L334) 把当前 `queue.value` 作为 `prefixActions` 传给 `aimTargetingStore.enter`，后端基于此前缀做射程推算。目标选定后才在 [onTargetSelect:378](file:///d:/wamp64/www/phpdts/vex-vue/src/components/battle/PreloadArea.vue#L378) 调用 `addToQueue` 入队。

若强行让 pending 项入队，`prefixActions` 会包含一个 target 为 `{type:'pending'}` 的无效项，后端射程推算会从空目标出发，得到错误结果。

**v2 新基准**：半成品态**不修改 `queue`**，而是在 PreloadArea 内部维护一个独立的 `pendingItem: Ref<PendingItem | null>`。`PendingItem` 是虚拟显示项，参与轨道渲染但不参与 `prefixActions`。

- **入队流程**（aimType=tile/enemy）：
  1. 用户点击技能 → 创建 `pendingItem = { act_id, target: { type: 'pending' } }`
  2. 轨道顶部渲染该虚拟项（序号 = queue.length + 1，半成品态样式）
  3. `enterAimMode` 的 `prefixActions` 仅传 `queue.value`，不含 pendingItem
  4. `onTargetSelect` 时：`addToQueue(act_id, selectedTarget)` → `pendingItem = null`
  5. 用户点击 Breadcrumb 的 `[取消]` 按钮：`pendingItem = null`，exitAimMode

- **重新瞄准流程**（B4）：
  1. 用户点击已入队项的目标 chip
  2. 该项从 `queue` 中**临时移除**，存入 `pendingItem`，原 target 暂存 `pendingItem._prevTarget`，原位置暂存 `pendingItem._prevIndex`
  3. `enterAimMode` 的 `prefixActions` 传**移除该项后的** `queue.value`（语义正确：射程推算假设此前项已完成）
  4. `onTargetSelect`：把新 target 通过 `splice(_prevIndex, 0, ...)` 回插原位置（保留原 id，不调用 `addToQueue`，详见 §5.3 步骤 5 与 §7.3 决断）
  5. 用户点击 Breadcrumb 的 `[取消]` 按钮：把 `pendingItem._prevTarget` 通过 `splice(_prevIndex, 0, ...)` 回插原位置（同上，保留原 id）

**对 B3 的解决**：`pendingItem` 是渲染层 sticky 显示的来源。Breadcrumb 即该项的视觉表现。

**对 B4 的解决**：任意已入队项的目标 chip 都可点击 → 项临时移入 `pendingItem` → 重新瞄准 → 替换。无需删除重加。

**协议扩展需求**：无需修改 `aimTargetingStore.enter` 的签名。重新瞄准时 PreloadArea 先把该项从 `queue` 中移除，再传**移除后的** `queue.value` 作为 `prefixActions`，语义已正确（射程推算假设此前项已完成）。无需引入 `excludeItemId` 参数—— [aim-targeting.ts:30-34](file:///d:/wamp64/www/phpdts/vex-vue/src/stores/aim-targeting.ts#L30-L34) 的 `AimTargetingSession` 接口也不存在该字段。

**概念分级**：模块 → 基准框架（队列项状态机：草稿/半成品/锁定/已提交）

### 2.3 AP 预算槽（替代"AP 进度条"）

**问题本质**：当前用连续 progress bar + 红色扣除段表达 AP。但 AP 本质是离散预算——N 个 AP 槽，每回合重置，队列锁定 M 个，剩余 N-M 可用。progress bar 语言把离散量当连续量处理，C2 缺分段刻度只是这一概念错位的症状。

**新基准**：AP 显示为 **N 个独立方块**（maxAp 个），每方块三态：

- **可用**：虚线空槽（灰阶 + 1px 虚线边）
- **队列锁定**：斜线纹理（`repeating-linear-gradient(45deg)` 灰阶）+ 对应队列项序号
- **溢出超载**：若 queueCost > playerAp，超出的槽位显示为带 `╳` 标记的禁止槽

**对 C2 的解决**：N 个独立方块本身就是天然的"分段刻度"——5 个槽就是 5 等分，3 个槽就是 3 等分，无需额外 `::after` 分段。

**视觉联动**（关键）：hover 队列项时，对应的 AP 槽高亮，让"动作 → 消耗"关系一眼可见。

**概念分级**：模块 → 基准框架（预算槽三态编码）

### 2.4 原子队列控制（替代"清空全部"）

**问题本质**：当前只有 `clearQueue()` 和 `removeFromQueue(i)` 两个粗糙操作。但玩家在规划时序时，需要：

- 撤销最后一项（B5+ 核心）
- 删除任意指定项
- 修改任意项的目标（B4）
- 调整任意项的顺序（D4）

**新基准**：装填轨道的每项都是可独立操作的原子单元。操作层级：

| 操作 | 触发方式 | 作用对象 |
|------|---------|---------|
| 撤销末项 | 队列末项的 `[x]` 按钮 | 最后一项 |
| 删除指定项 | 项的 `[x]` 按钮 | 该项 |
| 修改目标 | 点击项的目标 chip | 该项进入半成品态 |
| 调整顺序 | 拖拽项的手柄 | 非 finisher 项 |
| 清空全部 | `[清空]` 按钮（二次确认） | 全部 |

**安全约定**：清空全部需要二次确认（按钮变 `[确认清空?]` 高亮态，3 秒窗口内再次点击执行，详见 §5.1），避免误操作丢失整轮规划。

**概念分级**：模块 → 设计意图（细粒度操作 + 二次确认安全网）

---

## 三、信息架构

### 3.1 PreloadArea 分区重构

**v3 布局调换**：与探索页面"信息在上、操作在下"的操作习惯同步——装填轨道（已规划信息）在上方，技能库（选择操作源）在下方。原 v2 布局"技能库在上、装填轨道在下"已调换。

```
┌─────────────────────────────────────┐
│ [Breadcrumb 区]（条件渲染，sticky）  │ ← 半成品时显示
├─────────────────────────────────────┤
│  装填轨道（信息区）                  │ ← D2/D3/D4
│  1 ▶ [空手攻击] → 目标A  AP:1  [x] │
│  2 ▶ [投掷]     → 目标B  AP:2  [x] │
│  3 ▶ [处决]     → 目标C  AP:3  [F] │
│  ──                                │
│  [执行] [清空]                      │ ← 紧邻轨道，规划确认
├─────────────────────────────────────┤
│  AP 槽（格子矩阵）                  │ ← C2
│  ▢ ▣ ▣ ▢ ▢   （5/3/0 表示 5 总 3 锁）│
│  AP 3/5 · 锁定 3 · 可用 2           │ ← D5 聚合
├─────────────────────────────────────┤
│  技能库（操作区，flex-1 可滚动）     │ ← B1
│  [全部] 机动  攻击  辅助  终结       │ ← 标签页，默认"全部"
│  ──                                │
│  [移动]      AP:1 R:3   机动       │
│  [逃跑]      AP:2       机动       │
│  [空手攻击]  AP:1 R:1   攻击       │
│  [投掷]      AP:2 R:3   攻击       │
│  [治疗]      AP:2       辅助       │
│  [处决]      AP:3   [F] 终结       │ ← D3
│  ...                               │
└─────────────────────────────────────┘
```

**分区语义**：
- **信息区（上方）**：Breadcrumb + 装填轨道 + AP 槽。展示"当前规划状态"——已选动作序列、预算消耗、半成品提示。
- **操作区（下方）**：技能库。展示"可选操作源"——按分类分组的技能列表，供用户选择添加到轨道。
- **[执行][清空] 紧邻装填轨道底部**：这两个按钮是"对装填轨道的确认/清空操作"，逻辑上属于信息区而非操作区。规划完轨道后立即点 [执行]，无需滚到最底部。

**滚动行为**：
- 装填轨道通常项数有限（一回合动作数 ≤ maxAp/最小cost），不单独滚动。
- 技能库项数可能较多（9+ 技能 + 分类标题），作为 `flex-1` 区域占据剩余高度并独立滚动。
- 整个 PreloadArea 由 [BattleActionBar.vue:20](file:///d:/wamp64/www/phpdts/vex-vue/src/components/battle/BattleActionBar.vue#L20) 的父容器 `flex-1 overflow-y-auto min-h-0` 托管滚动。

### 3.2 技能库标签页筛选（B1）

**v3 改动**：v2 的"分组渲染"（按分类切片成多个分组块）改为**标签页筛选**（顶部标签栏 + 单一扁平列表）。理由：分组渲染只是"视觉章节标题"，无筛选能力，技能数增加后仍需滚动找；标签页提供主动筛选，且与探索页面"操作区紧凑"的语义更契合。

**标签页结构**：

```
[全部] 机动  攻击  辅助  终结
──
[技能1] AP:.. R:..  [分类小标签]
[技能2] ...
```

- **标签集合**：`全部 | maneuver | assault | support | finisher`，共 5 个
- **默认选中**：`全部`——首次进入保持全局视野，避免默认筛选造成"技能少了"的错觉
- **激活态样式**：先用简单下划线激活态（见 §4.2），后续调整为方案 C 硬投影
- **非激活态**：灰阶文字，hover 显白

**筛选实现**：

```ts
const activeCategory = ref<'all' | SkillCategory>('all');

const filteredSkills = computed(() => {
  if (activeCategory.value === 'all') return visibleSkills.value;
  return visibleSkills.value.filter(
    s => getSkillCategory(s.act_id) === activeCategory.value
  );
});
```

**分类映射表**（保留 §3.2 原映射，用途从"分组渲染"改为"标签筛选 + 项尾小标签"）：

按 `act_id` 推导分类，映射表写在 `skill-templates.ts`（与显示模板同源，避免分散）：

```ts
type SkillCategory = 'maneuver' | 'assault' | 'support' | 'finisher';

const SKILL_CATEGORY: Record<string, SkillCategory> = {
  move: 'maneuver',
  escape: 'maneuver',
  unarmed_strike: 'assault',
  throw: 'assault',
  whirlwind: 'assault',
  vampiric_bite: 'assault',
  grenade: 'assault',
  heal: 'support',
  execute: 'finisher',
};
```

**未注册 act_id 的 fallback**：默认归入 `assault`，避免新技能无家可归。

**标签顺序**：`全部 → maneuver → assault → support → finisher`。理由：`全部` 在最左作为默认入口；其余按"位置/状态切换 → 主动攻击 → 辅助 → 终结"的认知流程排列；终结技放末尾视觉上呼应"必须最后执行"。

**项尾小标签**：每个技能项右侧显示分类小标签（`机动` / `攻击` / `辅助` / `终结`），灰阶小字。作用：在 `全部` 标签下也能一眼识别分类；在筛选标签下作为视觉冗余确认。finisher 项的项尾小标签可与 `[F]` 标记合并（避免重复）。

**标签栏滚动**：5 个标签在战斗面板宽度内可容纳（每个约 30-40px）。若未来标签数增加，标签栏自身可横向滚动，本设计案不实现。

### 3.3 装填轨道项结构（D2/D3/D4/D5）

每个轨道项含 5 个视觉区块：

```
┌─────────────────────────────────────────────┐
│ [手柄] 1 ▶ [技能名] → [目标chip] [AP:2] [x] │
└─────────────────────────────────────────────┘
   ↑     ↑       ↑           ↑        ↑     ↑
  拖拽  序号   技能标签     目标    成本  删除
```

- **手柄**：左侧 2px 灰条，hover 显白，cursor: grab。finisher 项无手柄（不可拖）。
- **序号**：大字号 `1/2/3...`，灰阶 #fff，字重 700。
- **技能名**：`[技能名]` 方括号包裹（保持终端语言）。
- **目标 chip**：可点击（重新瞄准）。文本：`→ 位置 7` / `→ 兔子` / `→ 自己` / `→ 无`。半成品态：`→ [选择中...]`（脉冲）。
- **AP 徽标**：`AP:2` 灰阶，hover 时与对应 AP 槽联动。
- **finisher 标记**：`[F]` 标签，位于 AP 右侧，1px 白边框 + 2px 硬投影（方案 C 样式）。
- **删除按钮**：`[x]`，hover 时显白。

### 3.4 Breadcrumb 区（B3）

进入瞄准模式（即 `pendingItem !== null` 时），PreloadArea 顶部 sticky 显示：

```
┌─────────────────────────────────────────┐
│ ▶ 瞄准中：[投掷] · R:3 · [取消]          │
└─────────────────────────────────────────┘
```

样式：方案 C 标题 + 左侧 2px 白色脉冲色条（≤3Hz）。背景半透明遮罩，确保 PreloadArea 内容超出视口滚动时始终可见。

**v3 布局调整后的 sticky 语义**：装填轨道在 Breadcrumb 下方、信息区顶部。Breadcrumb sticky 在 PreloadArea 滚动容器顶部，无论用户滚动浏览技能库还是装填轨道，半成品态提示始终可见。这是 v3 布局的核心收益之一——v2 布局下 Breadcrumb 在技能库上方，滚动技能库时 Breadcrumb 会跟随技能库一起上滚消失；v3 布局下 Breadcrumb 在最顶部，sticky 行为更自然。

**v2 父容器约束补充**（v1 遗漏）：[BattleActionBar.vue:20](file:///d:/wamp64/www/phpdts/vex-vue/src/components/battle/BattleActionBar.vue#L20) 的父容器 `<div class="flex-1 overflow-y-auto min-h-0">` 是滚动容器。`position: sticky; top: 0` 在 `overflow-y-auto` 容器内表现与全屏 sticky 不同——它会粘附在该滚动容器顶部，而非视口顶部。这正是本设计案需要的行为（Breadcrumb 粘附在面板顶部）。但需确保父容器无 `overflow: hidden` 祖先，否则 sticky 失效。

**`[取消]` 按钮**：点击触发 §5.3 的取消流程（恢复原 target 或丢弃 pendingItem）。**v2 修正**：v1 标注为 `[ESC 取消]` 暗示快捷键，v2 改为 `[取消]` 按钮形式，符合本设计案不引入快捷键的策略。

---

## 四、视觉设计

### 4.1 美学约束

严格遵循 [前端美学风格-设计案.md](前端美学风格-设计案.md) 与融合 demo：

- **黑白单色**：禁用红/绿/黄信号色。`(-3)` 红色警告改为括号 + 减号本身已足够；移除按钮 hover 用 `╳` 形状加粗替代红色。
- **形状优先**：AP 槽三态用"虚线/斜线/禁止符"区分，不靠颜色。
- **剪纸层次**：轨道项用方案 C 边框 + 2px 硬投影，finisher 标记同款。
- **ASCII 终端**：技能名保持方括号包裹，目标 chip 用 `→` 箭头。
- **reduced-motion**：所有 hover 过渡、AP 槽高亮、脉冲都需 `prefers-reduced-motion` 降级。

### 4.2 技能库项样式（B1）

#### 4.2.1 标签栏样式（v3 新增，简单实现）

先用最简下划线激活态，后续调整为方案 C 硬投影：

```css
.skill-tabs {
  display: flex;
  gap: 12px;
  padding: 4px 0 6px;
  border-bottom: 1px solid var(--border-thin);
  margin-bottom: 4px;
}
.skill-tab {
  background: transparent;
  border: none;
  color: #666;
  font-size: 11px;
  padding: 2px 0;
  cursor: pointer;
  border-bottom: 1px solid transparent;
  transition: color 0.12s, border-color 0.12s;
}
.skill-tab:hover { color: #ddd; }
.skill-tab.is-active {
  color: #fff;
  border-bottom-color: #fff;
}
```

**后续调整方向**：激活态改为方案 C（1px 白边 + 2px 硬投影），与 finisher 标记、`[确认清空?]` 高亮态统一。本设计案实现阶段先用上述简单样式。

#### 4.2.2 技能库项样式

参照 demo `.tile-row` 模式（[ui-style-blended.html:362-390](file:///d:/wamp64/www/phpdts/oblivions/docs/demos/ui-style-blended.html#L362-L390)）：

```css
.skill-row {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 8px 6px 10px;
  border: 1px solid var(--border-thin);
  margin-bottom: 2px;
  color: #bbb;
  cursor: pointer;
  transition: border-color 0.12s, background 0.12s;
  position: relative;
  overflow: hidden;
}
.skill-row::before {
  content: '';
  position: absolute;
  left: 0; top: 0; bottom: 0;
  width: 2px;
  background: #666;
  transition: background 0.12s;
}
.skill-row:hover { border-color: #ddd; background: rgba(255,255,255,0.04); }
.skill-row:hover::before { background: #fff; }
.skill-row:disabled { opacity: 0.3; cursor: not-allowed; }
.skill-row:disabled:hover { border-color: var(--border-thin); background: transparent; }
.skill-row:disabled:hover::before { background: transparent; }
.skill-row:active { transform: translateY(1px); box-shadow: inset 0 2px 4px rgba(0,0,0,0.6); }

.skill-row .meta { color: #666; font-size: 10px; margin-left: auto; }
.skill-row .category-tag {
  color: #555;
  font-size: 9px;
  margin-left: 6px;
  letter-spacing: 0.05em;
}
.skill-row .finisher-tag {
  margin-left: 6px;
  padding: 1px 6px;
  border: 1px solid rgba(255,255,255,0.35);
  background: var(--bg-base);
  box-shadow: 2px 2px 0 rgba(0,0,0,0.3);
  color: #fff;
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.1em;
}
```

### 4.3 AP 槽格子矩阵（C2）

替代当前 `.ap-bar-container` 进度条：

```css
.ap-slots {
  display: flex;
  gap: 3px;
  margin: 4px 0;
}
.ap-slot {
  width: 14px;
  height: 14px;
  border: 1px dashed rgba(136,136,136,0.4);  /* 可用态：虚线 */
  background: transparent;
  transition: border-color 0.12s, background 0.12s;
  position: relative;
}
.ap-slot.is-locked {
  border-style: solid;
  border-color: rgba(255,255,255,0.4);
  background-image: repeating-linear-gradient(
    45deg,
    rgba(255,255,255,0.25) 0 2px,
    transparent 2px 5px
  );
}
.ap-slot.is-locked::after {
  content: attr(data-order);  /* 显示对应队列项序号 */
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 9px;
  font-weight: 700;
}
.ap-slot.is-overload {
  border-color: rgba(255,255,255,0.6);
  border-style: double;
}
.ap-slot.is-overload::after {
  content: '╳';
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 12px;
  font-weight: 400;
  opacity: 0.7;
}
.ap-slot.is-highlighted {
  border-color: #fff;
  box-shadow: 0 0 0 1px rgba(255,255,255,0.3);
}
```

**分段刻度**：N 个槽就是 N 等分，无需额外 `::after`。视觉上 5 个槽一眼可读为"5 等分"。

### 4.4 装填轨道项样式（D2/D3/D4）

```css
.queue-track-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 8px 6px 12px;
  border: 1px solid var(--border-thin);
  margin-bottom: 2px;
  background: var(--bg-base);
  position: relative;
  transition: border-color 0.12s, background 0.12s, box-shadow 0.12s;
}
.queue-track-item::before {
  content: '';
  position: absolute;
  left: 0; top: 0; bottom: 0;
  width: 2px;
  background: #888;
}
.queue-track-item:hover {
  border-color: #ddd;
  background: rgba(255,255,255,0.04);
}
.queue-track-item:hover::before { background: #fff; }
.queue-track-item.is-pending::before {
  background: #fff;
  animation: pulse-pending 1.2s ease-in-out infinite;
}
.queue-track-item.is-finisher {
  border-color: rgba(255,255,255,0.35);
  box-shadow: 2px 2px 0 rgba(0,0,0,0.3);  /* 方案 C 硬投影 */
}
.queue-track-item.is-dragging {
  opacity: 0.5;
  border-style: dashed;
}
.queue-track-item.is-drop-target {
  border-color: #fff;
  border-style: dashed;
  background: rgba(255,255,255,0.08);
}

.queue-track-item .order {
  color: #fff;
  font-weight: 700;
  font-size: 12px;
  min-width: 16px;
}
.queue-track-item .target-chip {
  cursor: pointer;
  color: #888;
  font-size: 10px;
  border-bottom: 1px dashed rgba(136,136,136,0.4);
  transition: color 0.12s, border-color 0.12s;
}
.queue-track-item .target-chip:hover {
  color: #fff;
  border-bottom-color: #fff;
}
.queue-track-item .target-chip.is-pending {
  color: #fff;
  animation: pulse-pending 1.2s ease-in-out infinite;
}
.queue-track-item .drag-handle {
  cursor: grab;
  color: #555;
  user-select: none;
}
.queue-track-item.is-finisher .drag-handle {
  cursor: not-allowed;
  opacity: 0.2;
}

@keyframes pulse-pending {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.4; }  /* ≤3Hz，符合光敏约束 */
}

@media (prefers-reduced-motion: reduce) {
  .queue-track-item.is-pending::before,
  .queue-track-item .target-chip.is-pending {
    animation: none;
    opacity: 0.6;
  }
  .obl-btn.turn-active { animation: none; }
}
```

### 4.5 总 AP 聚合徽标（D5）

AP 槽下方聚合行，替代当前 `5 → 2 (-3)`：

```
▢ ▣ ▣ ▢ ▢
AP 5/5 · 锁定 3 · 可用 2
```

- `5/5` = 当前/最大
- `锁定 3` = queueCost
- `可用 2` = predictedAp
- 超载时（queueCost > playerAp）：`可用 0 · 超载 1`，超载数字加 `╳` 形状强调（不加红色）

---

## 五、交互设计

> **键盘交互延后声明**：本设计案所有交互均通过鼠标/触摸完成，不引入键盘快捷键。撤销末项、删除指定项、清空全部均通过 UI 按钮触发。键盘快捷键的统一引入时机见文档顶部声明。

### 5.1 鼠标交互

**技能库项**：
- 点击 → 进入瞄准（aimType=tile/enemy）或直接入队（aimType=none/self）
- hover → 显示边框提亮 + 左色条显白（无射程预览，远期扩展）

**轨道项**：
- 点击目标 chip → 该项进入半成品态（重新瞄准，B4，详见 §5.3）
- 点击 `[x]` → 删除该项
- 拖拽手柄 → 调整顺序（finisher 项不可拖，D4，详见 §5.2）

**AP 槽**：
- hover 队列项 → 对应 AP 槽高亮（联动，事件委托实现见 §5.4）
- 点击 AP 槽 → 无作用（视觉元素，不可交互）

**清空全部的二次确认**（替代 v1 的 ESC 连按）：
1. 点击 `[清空]` 按钮 → 按钮变为 `[确认清空?]` 高亮态（方案 C 边框 + 脉冲）
2. 3 秒内再次点击 → 执行清空
3. 3 秒外或点击其他区域 → 取消确认，按钮复原

### 5.2 拖拽重排规则（D4）

**可拖拽项**：非 finisher 的轨道项。

**可放置位置**：
- 任意非末尾位置（若队列含 finisher，finisher 之前的任意位置）
- 末尾位置（若队列不含 finisher）

**不可放置**：
- finisher 项之后（finisher 必须末尾约束）

**实现**：HTML5 Drag and Drop API（`draggable` + `dragstart`/`dragover`/`drop`）。拖拽中源项 `is-dragging` 半透明，目标位置显示 `is-drop-target` 虚线高亮。

**reduced-motion 降级**：拖拽本身是用户主动操作，不受 reduced-motion 影响。但 drop-target 高亮的过渡动画在 reduced-motion 下立即应用。

**移动端 fallback**（远期扩展）：mobile 端用"长按 → 弹出操作菜单（上移/下移/删除/重瞄准）"替代拖拽，本设计案不实现。

### 5.3 重新瞄准流程（B4）

依据 §2.2 的"虚拟 pending 项"方案：

1. 用户点击某已入队项的目标 chip
2. 该项从 `queue` 临时移除，存入 `pendingItem`（含 `_prevTarget`）
3. `enterAimMode` 调用，`prefixActions` 传**移除该项后的** `queue.value`
4. Breadcrumb 渲染 `pendingItem`（"瞄准中：[技能名] · R:X · [取消]"）
5. 用户选定新目标：
   - **回原位置**（§7.3 已决断）：与取消恢复同路径，使用 `queue.value.splice(pendingItem._prevIndex, 0, { id: pendingItem.id, act_id: pendingItem.act_id, target: newTarget, status: 'draft' })` 直接插回原位，保留原 id
   - **不调用 `addToQueue`**——同 §6 步骤的理由（避免新 id 与 finisher 插入逻辑误触发）
   - `pendingItem = null`，exitAimMode
6. 用户点击 `[取消]` 或点击 Breadcrumb 外区域：
   - **保留原 id 与原位置**：使用 `queue.value.splice(pendingItem._prevIndex, 0, { id: pendingItem.id, act_id: pendingItem.act_id, target: pendingItem._prevTarget, status: 'draft' })` 直接插回原位
   - **不调用 `addToQueue`**——`addToQueue` 会通过 `queueIdSeed++` 生成新 id，破坏 id 连续性，且会触发"finisher 插入到末尾前"等特殊逻辑，与"恢复原位置"语义冲突
   - `pendingItem = null`，exitAimMode

**决断（§7.3）**：重新入队回原位置以保留用户的时序规划。`pendingItem._prevIndex` 暂存原位置，使用 `splice(_prevIndex, 0, ...)` 回插，保留原 id。move 类技能重算由 `queueWithCost` computed 自动处理（原位置前的项未变化，plannedLoc 不变，重算结果一致）。

**数据保护**：进入半成品态前，原 target 暂存在 `pendingItem._prevTarget`，取消时恢复。

**目标已死亡/离开的边界**：`findSelectableCombatTarget` 返回 null 时，保持 `pendingItem` 半成品态 + toast 提示"目标不可选，请重新选择"。

### 5.4 hover 联动实现（AP 槽 ↔ 队列项）

**v2 优化**：使用事件委托而非每项绑定。

```ts
// 父容器绑定 mouseover/mouseout
function onQueueTrackOver(e: MouseEvent) {
  const item = (e.target as HTMLElement).closest('[data-order]');
  hoveredOrder.value = item ? Number(item.dataset.order) : null;
}
```

CSS 通过 `:has()` 或 JS 类切换实现 AP 槽高亮：

```css
.ap-slot[data-order="3"].is-locked {
  /* 默认锁定态 */
}
.ap-slots:has(.queue-track-item[data-order="3"]:hover) .ap-slot[data-order="3"] {
  border-color: #fff;
  box-shadow: 0 0 0 1px rgba(255,255,255,0.3);
}
```

`:has()` 浏览器支持需评估（Chrome 105+、Safari 15.4+、Firefox 121+）。不支持时回退到 JS 类切换。

---

## 六、数据结构与状态

### 6.1 QueueItem 与 PendingItem

**v2 修正**：现有 [PreloadArea.vue:42-46](file:///d:/wamp64/www/phpdts/vex-vue/src/components/battle/PreloadArea.vue#L42-L46) 的 `QueueItem` 仅含 `id/act_id/target` 三字段，**不引入** `apCost`/`isFinisher`/`category` 等缓存字段（缓存字段在队列顺序变化时需手动 `recalcQueueCosts()` 同步，破坏 Vue 响应式），改为 computed 派生。

```ts
type QueueItemStatus = 'draft' | 'locked' | 'submitted';

interface QueueItem {
  id: number;
  act_id: string;
  target: CombatAimIntent;
  status: QueueItemStatus;
}

// 半成品虚拟项，不入 queue，独立 ref
interface PendingItem {
  id: number;
  act_id: string;
  target: { type: 'pending' };
  _prevTarget?: CombatAimIntent;  // 重新瞄准时暂存原 target
  _prevIndex?: number;            // 重新瞄准时暂存原位置
}

const pendingItem = ref<PendingItem | null>(null);
```

**派生字段（computed，响应式）**：

> **v2 修正**：以下 4 个函数在现有代码中**均不存在**，需新增（实现时建议放 `utils/skill-category.ts` 与 `utils/combat-action-cost.ts`）：
> - `isFinisherSkill(act_id)` — 按 §3.2 的 `SKILL_CATEGORY` 映射判断
> - `getSkillCategory(act_id)` — 同上，返回 `SkillCategory`
> - `estimateActionCostFrom(act_id, target, plannedLoc)` — 通用成本估算；对 `move` 类分支调用现有 [estimateMoveActionCost](file:///d:/wamp64/www/phpdts/vex-vue/src/utils/combat-action-cost.ts#L10-L15)（其签名为 `(skill: Skill, distance: number | null)`），其他类按 `act_id` 返回基础或查表成本
> - `getPlannedActorPls(prefixQueue)` — 按 prefix 队列推算玩家在此项执行前的位置（move 类需要）

```ts
const queueWithCost = computed(() =>
  queue.value.map((item, index) => {
    const plannedLoc = getPlannedActorPls(queue.value.slice(0, index));
    return {
      ...item,
      apCost: estimateActionCostFrom(item.act_id, item.target, plannedLoc),
      isFinisher: isFinisherSkill(item.act_id),
      category: getSkillCategory(item.act_id),
      order: index + 1,
    };
  })
);

const queueCost = computed(() =>
  queueWithCost.value.reduce((sum, item) => sum + item.apCost, 0)
);

const isOverload = computed(() => queueCost.value > playerAp.value);
```

**move 重算的自动性**：因为 `queueWithCost` 是 computed，队列顺序变化（拖拽）会自动重算所有项的 `apCost`。无需手动调用 `recalcQueueCosts()`——v1 的手动调用是反 Vue 模式。

### 6.2 AP 槽派生

```ts
interface ApSlotState {
  index: number;
  state: 'available' | 'locked' | 'overload';
  lockedByOrder?: number;
}

const apSlots = computed<ApSlotState[]>(() => {
  const slots: ApSlotState[] = [];
  for (let i = 0; i < playerMaxAp.value; i++) {
    if (i < playerAp.value - queueCost.value) {
      slots.push({ index: i, state: 'available' });
    } else if (i < playerAp.value) {
      slots.push({ index: i, state: 'locked', lockedByOrder: deriveOrderForAp(i) });
    } else {
      slots.push({ index: i, state: 'overload' });
    }
  }
  return slots;
});
```

### 6.3 submitted 状态与 isPlayerTurn 钩子（v2 补充）

**v1 遗漏**：未定义 submitted 状态的进入/退出时机。

**v2 定义**：通过 watch `battleStore.isPlayerTurn` 实现：

```ts
watch(() => battleStore.isPlayerTurn, (isMyTurn, wasMyTurn) => {
  if (wasMyTurn && !isMyTurn) {
    // 玩家回合结束 → 锁定队列
    queue.value.forEach(item => { item.status = 'submitted'; });
  } else if (!wasMyTurn && isMyTurn) {
    // 新回合开始 → 清空队列，重新可编辑
    queue.value = [];
    pendingItem.value = null;
  }
});
```

**进入 submitted 的时机**：执行命令成功提交后（`onExecute` 的 await 返回后），所有 queue 项 status 置为 `submitted`。UI 锁定，禁止任何修改操作（拖拽、点击、删除按钮禁用）。

**退出 submitted 的时机**：
- 主路径：`isPlayerTurn` 从 true → false → true（新回合开始），自动清空
- 异常路径：战斗结束（`battleStore.isBattleOver`）或玩家死亡，PreloadArea 卸载

**对应的 `skill_list` 失效**：`onPreloadExecuted` 已在 [battle.ts:1055-1061](file:///d:/wamp64/www/phpdts/vex-vue/src/stores/battle.ts#L1055-L1061) 调用 `dataManager.invalidate('player_info')` 等。但 `skill_list` 当前只在 `initPreloadArea` 时调用，新回合开始时若 AP/CD 变化需重新拉取。建议在 `isPlayerTurn` watch 中追加 `dataManager.invalidate('skill_list')`。

---

## 七、边界案例、待决策与已决断

### 7.1 边界案例

1. **move 技能拖拽导致后续 move 距离失效**：move 的 apCost 依赖从 plannedLoc 到目标的距离。`Math.max(baseCost, Math.ceil(distance / movePower))` 的下限保护见 [combat-action-cost.ts:10-14](file:///d:/wamp64/www/phpdts/vex-vue/src/utils/combat-action-cost.ts#L10-L14)。v2 通过 `queueWithCost` computed 自动重算，无需手动 `recalcQueueCosts()`。
   - **字段复用语义**（v1 遗漏）：`movePower` 实际取自 `getSkillBaseRange(skill)` 即 `skill.range.base`，move 类技能的 "range" 同时表示"射程"和"单位 AP 移动距离"。这是历史设计复用，非 bug，但需在开发文档中注明。
2. **重新瞄准时原目标已死亡/离开**：`findSelectableCombatTarget` 返回 null 时，保持 `pendingItem` 半成品态 + toast 提示。
3. **finisher 后插入新项**：当前 `addToQueue` [PreloadArea.vue:399-413](file:///d:/wamp64/www/phpdts/vex-vue/src/components/battle/PreloadArea.vue#L399-L413) 已实现"普通技插入到 finisher 之前"。v2 升格为 `useTemporalPlan` 的内置约束（见 §10 升格分级），不再是 PreloadArea 的特殊逻辑。
4. **超载 AP 提交**（v2 修正）：**不**通过 `executeBlock` 实现（[command-queue.ts](file:///d:/wamp64/www/phpdts/vex-vue/src/stores/command-queue.ts) 的 5 层锁体系不含 AP 检查）。改为 PreloadArea 内部 UI 派生：
   ```ts
   const executeDisabled = computed(() =>
     executeBlock.value !== null || isOverload.value || pendingItem.value !== null
   );
   ```
   超载时 `[执行]` 按钮 disabled + tooltip "AP 不足，请减少装填"。
5. **pre-battle 模式 vs in-battle 模式**：两种模式的拖拽/重新瞄准行为一致，差异仅在执行命令不同（`battle.start` vs `battle.submit_turn`）。通过 `executeCommand` 配置注入。

### 7.2 待决策

1. **技能分类映射是否由后端返回**：当前 `SKILL_CATEGORY` 在前端硬编码。建议短期硬编码，长期由 `skill_config.php` 返回 `category` 字段。
2. **AP 槽 maxAp 较大时（>10）**：5 个 14px 方块在 70px 宽度内合理，10 个就是 140px。**v2 优化**：改为响应式——通过 `ResizeObserver` 或 `useElementSize` 监听容器宽度，自动切换紧凑模式（方块宽 10px + gap 2px），而非硬编码阈值 8。
3. **键盘焦点环样式**（远期，键盘功能落地时）：`focus-visible` 应用方案 C（1px 白边 + 2px 硬投影）还是 demo 的 `box-shadow: 0 0 0 1px rgba(255,255,255,0.08)`？建议方案 C，与 finisher 标记统一。

### 7.3 已决断

1. **重新瞄准后回原位置 vs 末尾**：**已决断**——回原位置（§5.3 步骤 5 已落实）。`pendingItem` 暂存 `_prevIndex`，新目标入队或取消恢复都用 `splice(_prevIndex, 0, ...)` 直接插回原位，保留原 id，避免 `addToQueue` 触发新 id 与 finisher 插入逻辑。move 类技能重算由 `queueWithCost` computed 自动处理（原位置前的项未变化，故 plannedLoc 不变，重算结果一致）。

---

## 八、实现路径

### 阶段 1：数据结构与状态机

- 扩展 `QueueItem` 类型（保留 `id/act_id/target`，新增 `status: QueueItemStatus`；不引入 `apCost`/`isFinisher`/`category` 缓存字段，改为 computed 派生）
- 引入 `PendingItem` 与 `pendingItem` ref
- **新增 4 个函数**（见 §6.1 声明）：`isFinisherSkill` / `getSkillCategory`（放 `utils/skill-category.ts`）、`estimateActionCostFrom` / `getPlannedActorPls`（放 `utils/combat-action-cost.ts`）。其中 `estimateActionCostFrom` 对 move 分支调用现有 `estimateMoveActionCost`，其他类按 `act_id` 返回基础或查表成本
- 实现 `queueWithCost` / `queueCost` / `isOverload` / `apSlots` computed
- 实现 `submitted` 状态与 `isPlayerTurn` watch 钩子
- 引入技能库筛选状态：`activeCategory: Ref<'all' | SkillCategory>` + `filteredSkills` computed（见 §3.2）

### 阶段 2：视觉重构

**v3 布局顺序**：按信息区（上方）→ 操作区（下方）的顺序实现，与 §3.1 ASCII 图一致。

- 重构 PreloadArea 顶层布局：信息区（Breadcrumb + 装填轨道 + [执行][清空] + AP槽）在上，操作区（技能库 flex-1）在下
- Breadcrumb 区实现（条件渲染 `pendingItem !== null`，sticky 定位需考虑父容器 `overflow-y-auto`——见 §3.4）
- 装填轨道区：重构 `.obl-btn` 队列项 → `.queue-track-item` 独立样式（§4.4）
- AP 槽区：删除 `.ap-bar-container` 进度条相关样式，新增 `.ap-slots` / `.ap-slot` 三态样式（§4.3）+ 总 AP 聚合徽标（§4.5）
- 技能库区：标签栏（5 个标签 + `activeCategory` ref + `filteredSkills` computed）+ 扁平列表渲染（v-for skill in filteredSkills）+ `.skill-row` 样式（§4.2）+ 项尾 `.category-tag` 小标签，作为 `flex-1` 占据剩余高度独立滚动

### 阶段 3：交互实现

- 拖拽重排（HTML5 Drag and Drop）
- 重新瞄准流程（点击 target-chip → pendingItem 临时移除 → enterAimMode）
- 清空全部的二次确认（按钮变 `[确认清空?]`，3 秒窗口）
- 超载 AP 的 `[执行]` 禁用与 tooltip

### 阶段 4：联动与降级

- hover 队列项 → AP 槽高亮联动（事件委托 + `:has()` 或 JS 类切换）
- 所有动画 `prefers-reduced-motion` 降级
- `pulse-pending` 等关键帧在 reduced-motion 下转为静态 opacity

---

## 九、与现有设计的对齐

### 9.1 与前端美学风格设计案的对齐

| 设计案约束 | 本设计案的实现 |
|----------|--------------|
| 禁用彩色信号色 | 红色 `(-3)` / `text-red` 全部移除，用括号 + 减号 + 斜线纹理替代 |
| 形状优先 | AP 槽三态用虚线/斜线/双线+╳ 区分；finisher 用 `[F]` 标签 |
| 立绘弹出作为唯一反馈 | 不引入新反馈机制，所有状态变化通过 CSS 过渡 |
| 循环频率 ≤3Hz | `pulse-pending` 1.2s 周期 ≈ 0.83Hz |
| reduced-motion 兜底 | 阶段 4 显式实现 |

### 9.2 与融合 demo 的对齐

| demo 元素 | 应用位置 |
|----------|---------|
| 方案 C 标题（1px 白边 + 2px 硬投影） | 标签页激活态（后续调整）、finisher 标记、`[确认清空?]` 高亮态 |
| `.tile-row` 左侧色条 hover 显白 | 技能库项、轨道项 |
| `.slot-card` 三态（empty/filled/hover） | AP 槽（available/locked/highlighted） |
| `.term-btn:active` inset shadow + translateY | 技能库项 active 状态 |
| Toast 撕纸 clip-path | 重新瞄准失败 toast |

---

## 十、设计案自身分级（v2 补充）

按 AGENTS.md 的"模块 → 基准框架 → 设计意图 → 边界案例"四级分级，并标注是否可升格为复用框架：

### 10.1 基准框架级（本设计案内）

| 设计内容 | 分级 | 说明 |
|---------|------|------|
| 装填轨道的时序语义（§2.1） | 基准框架 | 时序计划是核心抽象 |
| 队列项状态机（§2.2、§6.1） | 基准框架 | draft/locked/submitted 三态 |
| AP 预算槽三态编码（§2.3） | 基准框架 | 离散预算的视觉编码 |

### 10.2 可升格为复用框架（v2 新增分级）

| 设计内容 | 升格目标 | 复用场景 |
|---------|---------|---------|
| 时序轨道（§2.1） | `useTemporalPlan<T>` composable | CD 冷却队列、buff 队列、装备预设编辑 |
| 队列项状态机（§2.2） | `EditableItemStatus` 通用类型 | 任何"先规划后执行"的 UI 场景 |
| AP 预算槽（§2.3） | `<DiscreteBudget>` 组件 | SP 等离散资源（**不适用于 HP**——HP 是连续量） |
| 二次确认（§5.1） | `useConfirmAction` composable | 丢弃装备、退出战斗等破坏性操作 |

**升格实施时机**：建议在 PreloadArea 重构完成后，作为独立的"前端基础框架抽象"任务提取。本设计案实现阶段直接使用上述抽象可减少后续重构成本，但若时间紧迫可先在 PreloadArea 内部实现，后续再抽离。

### 10.3 设计意图级

| 设计内容 | 分级 |
|---------|------|
| 原子队列操作（§2.4） | 设计意图 |
| 清空全部二次确认（§5.1） | 设计意图 |
| 超载 AP UI 派生禁用（§7.1.4） | 设计意图 |
| submitted 状态生命周期（§6.3） | 设计意图 |

### 10.4 模块级

| 设计内容 | 分级 |
|---------|------|
| 技能分类映射表（§3.2） | 模块 |
| 拖拽重排规则（§5.2） | 模块 |
| 重新瞄准流程（§5.3） | 模块 |
| hover 联动事件委托（§5.4） | 模块 |

### 10.5 边界案例级

| 设计内容 | 分级 | 处理方式 |
|---------|------|---------|
| move 距离失效重算（§7.1.1） | 边界案例 | 通过 `queueWithCost` computed 自动处理 |
| 重新瞄准原目标死亡（§7.1.2） | 边界案例 | toast + 保持半成品态 |
| finisher 后插入新项（§7.1.3） | **升格为基准框架内置约束** | 不再是边界案例，`useTemporalPlan` 内置 |
| pre-battle/in-battle 差异（§7.1.5） | 边界案例 | 配置注入 |
| 移动端拖拽替代（远期） | 边界案例 | 长按弹出操作菜单 |
| maxAp 紧凑模式（§7.2.3） | 边界案例 | 响应式 `ResizeObserver` 自动切换 |
