interface:
  display_name: "Oblivions 指南针"
  short_description: "通过设计锚点、代码图谱与运行时证据导航 PHPDTS"
  default_prompt: "使用 $oblivions-compass 定位 PHPDTS 的设计、代码、调用关系与运行时证据。"

dependencies:
  tools:
    - type: "mcp"
      value: "codebase-memory-mcp"
      description: "查询 PHPDTS 代码图谱中的定义、调用链、数据流与模块边界。"

policy:
  allow_implicit_invocation: true
